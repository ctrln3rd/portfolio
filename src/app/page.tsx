import Home from "@/components/home"
import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'austine mark',
  description: 'austine mark a techie with varoius tech skills'
}

export default function HomePage() {
  return <Home/>
}
